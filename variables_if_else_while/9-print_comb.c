#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - entry point
 *
 * Return: always 0 (success)
 */
int main(void)
{
int nombre;

for (nombre = '0'; nombre <= '9'; nombre++)
{
putchar(nombre);
putchar(',');
putchar(' ');
}
if (nombre = '9')
putchar('\n');

return (0);
}
