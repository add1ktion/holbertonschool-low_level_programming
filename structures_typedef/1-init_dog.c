#include <stdio.h>
#include "dog.h"
/** init_dog -
* @d: Dog
* @name: Name of dog
* @age: Age of dog
* @owner: Owner of dog
* Return:
*/
void init_dog(struct dog *d, char *name, float age, char *owner)
{
	struct dog;

	if (dog == NULL)
		return (NULL);

	dog->name = name;
	dog->age = age;
	dog->owner = owner;
	return (dog);
}
