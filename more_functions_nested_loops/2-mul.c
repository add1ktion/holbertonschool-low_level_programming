#include "main.h"
/**
* _isdigit - check if digit
* @c: character checked
* Return: 1 if digit 0 if not
*/
void print_numbers(void)
{
    int i;

    for (i = 0; i <= 9; i++)
    _putchar(i + '0');
    _putchar('\n');
}